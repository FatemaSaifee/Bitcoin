defmodule NetworkTest do
    use ExUnit.Case
    doctest Network

    @tag :network
    test "Network " do
        # assert String.length(GenerateRandomStrings.randomizer(5)) == 5
        assert true
        
    end


end
